function MovieDetails(runtime, genres, tagline, imdbId) {
    this.runtime = runtime;
    this.genres = genres;
    this.tagline = tagline;
    this.imdbId = imdbId;
}

export {MovieDetails};
export default MovieDetails;